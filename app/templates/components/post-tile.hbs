{{#link-to 'post' post.id}}<h2>{{post.title}} by {{post.user}}</h2>{{/link-to}}
<h6>{{post.date}}</h6>
{{#if isImageShowing }}
  <p><img src={{post.image}} alt={{post.title}} {{action 'imageHide'}}></p>
{{else}}
  <button {{action 'imageShow'}}>Show image</button>
{{/if}}
